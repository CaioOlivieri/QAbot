# Layer 1 — test_agent.py + CI
status: aberta
atualizado: 2026-06-02
 
Só DEPOIS da Layer 0 (testar/automatizar um agente que não usa metade do que
construiu seria automatizar um fluxo incompleto).
- tests/test_agent.py (ainda não existe no repo).
- GitHub Actions: ruff + pytest --cov no push/PR.
